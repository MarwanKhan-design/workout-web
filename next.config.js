/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // 'appDir' is not a valid option in recent Next.js versions and causes a warning.
    // Remove it to fix the invalid config warning.
  },
}

module.exports = nextConfig
